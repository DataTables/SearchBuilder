<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" order="2">

<css lib="datatables buttons searchbuilder datetime"/>
<js lib="datatables buttons searchbuilder datetime jszip pdfmake vfsfonts">
<![CDATA[

new DataTable('#example', {
	layout: {
		top1: 'searchBuilder',
		topStart: {
			buttons: ['copy', 'csv', 'excel', 'pdf', 'print']
		}
	}
});

]]>
</js>

<title lib="Buttons">Buttons integration</title>

<info><![CDATA[

The Buttons extension for DataTables can be initialised very easily though the `-init layout` option and `-feature buttons` feature. SearchBuilder is also initialised as a feature and the two can co-exist on the same DataTable with no issues, as shown in this example.

]]></info>

</dt-example>

