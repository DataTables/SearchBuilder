<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html-iso8601" table-class="display nowrap" order="2">

<css lib="datatables searchbuilder datetime"/>

<js lib="jquery datatables searchbuilder datetime">
<![CDATA[

$(document).ready(function() {
    $('#example').DataTable( {
        dom: 'Qlfrtip',
        stateSave: true
    });
});

]]>
</js>

<title lib="SearchBuilder">State Save Integration</title>

<info><![CDATA[

This example demonstrates the behaviour of `-init searchBuilder` when state saving is enabled using `-init stateSave`.

When enabled DataTables will store state information such as pagination position, display length, filtering and sorting. When the end user reloads the page the table's state will be altered to match what they had previously set up.

]]></info>

</dt-example>
