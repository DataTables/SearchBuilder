<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" table-class="display nowrap" order="3">

<css lib="datatables searchbuilder buttons datetime"/>

<js lib="jquery datatables searchbuilder buttons datetime">
<![CDATA[

$('#example').DataTable({
	layout: {
		topStart: {
			buttons: ['searchBuilder']
		}
	}
});

]]>
</js>

<js-vanilla>
<![CDATA[

new DataTable('#example', {
	layout: {
		topStart: {
			buttons: ['searchBuilder']
		}
	}
});

]]>
</js-vanilla>

<title lib="SearchBuilder">Button Initialisation</title>

<info><![CDATA[

This example demonstrates the behaviour of SearchBuilder when initialised using a button. The button and SearchBuilder can be configured using the `-init button.searchBuilder` config option. You can see this in action at [this example](buttonOptions.html). Please note that when initialising SearchBuilder with a button any config has to be set as shown in that example.

]]></info>

</dt-example>
